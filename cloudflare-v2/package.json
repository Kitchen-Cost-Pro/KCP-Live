{
  "name": "kcp-cloudflare-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test:recipe-persistence": "esbuild src/legacy/routes.ts --bundle --platform=node --format=esm --alias:cloudflare:sockets=./tests/cloudflare-sockets-shim.mjs --outfile=tests/.recipe-routes.bundle.mjs && node tests/recipe-persistence.integration.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260523.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.17.0"
  }
}
